# Permanent Edmund facade release gate

This is the same unmodified `workflow.zip` corpus used by the GUI baseline tests.
It contains the supplied 207-entry blank export, the earlier 187-entry translated
response, and the source files required to reproduce extraction. The source ZIP
does not include unrelated game assets.

The original export SHA-256 is
`7D47C214A567D0B096F614F4946C5BA1992A132EC41380ED4719B91E24DA2C67`.
`EdmundFacadeReleaseGateTest` is part of the normal project test suite and must
remain a release gate through subsequent UI and patch-output changes.
